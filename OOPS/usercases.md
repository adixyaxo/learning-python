# Summary Use Cases
1. Use Inheritance when you have different types of the same thing (e.g., CreditCard and DebitCard are both Cards).

1. Use Nesting (rarely) only when a class is a tiny helper strictly for one other class and never used elsewhere.

1. Use Composition (Separate classes) when one object consists of another (e.g., A Bank has Customers; an Account has Transactions).